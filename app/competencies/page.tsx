import { CompetenciesWorkspace } from "../../components/competencies/CompetenciesWorkspace";

export const metadata = {
  title: "Identity | ARES",
  description: "Keep long-term competencies and current focus seasons visible.",
};

export default function CompetenciesPage() {
  return <CompetenciesWorkspace />;
}
